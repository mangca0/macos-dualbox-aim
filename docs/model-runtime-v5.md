# V5 Model Runtime Notes

This file is kept as a compatibility pointer for older references. The current
model-runtime documentation lives in `docs/model-runtime.md`.

V5 proved the architecture now promoted to `core`:

- CoreML model contract inspection
- ImageType and MLMultiArray input handling
- CoreML NMS and raw YOLOv8 tensor adapters
- FP32 check and FP16 fast package workflow
- Realtime validation through `scripts/main_v5.py`

Use `docs/model-runtime.md` for future work.
